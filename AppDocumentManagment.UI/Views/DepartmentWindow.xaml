<Window x:Class="AppDocumentManagment.UI.Views.DepartmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppDocumentManagment.UI.Views"
        mc:Ignorable="d"
        Title="АИС Документооборот"  
        Height="280"
        Width="500"
        Icon="/Resources/Images/AppIcon.png" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40px"/>
            <RowDefinition Height="60px"/>
            <RowDefinition Height="60px"/>
            <RowDefinition Height="40px"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="#FF7D00FF" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <Label Grid.Row="0" 
                   x:Name="GreetingLabel"
                   Content="Панель редактирования отдела" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Center" 
                   FontFamily="Arial" 
                   FontSize="14" 
                   FontWeight="Bold"
                   Padding="20,0"
                   Foreground="WhiteSmoke"/>
            <Button x:Name="ExitBtn"
                    Content="Выйти"
                    HorizontalAlignment="Right"
                    Style="{StaticResource Buttons}"
                    FontFamily="Arial" 
                    FontSize="14" 
                    Margin="10, 5"
                    Padding="10, 0"
                    FontWeight="Bold"
                    Command="{Binding IExit}"/>
        </Grid>
        <Grid Grid.Row="1">
            <TextBox x:Name="DepartmentTitle" 
                     Grid.Row="1"
                     Style="{StaticResource ItemTitleBox}"
                     VerticalAlignment="Top"
                     FontSize="14"
                     Text="{Binding DepartmentTitle}"
                     Background="WhiteSmoke"
                     SpellCheck.IsEnabled="True"/>
            <Label IsHitTestVisible="False" 
                   Grid.Row="1"
                   Foreground="DimGray"
                   Content="Введите наименование отдела..."
                   VerticalAlignment="Top"
                   FontSize="14"
                   Visibility="{Binding Text.IsEmpty, ElementName=DepartmentTitle, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Margin="30 10"/>
        </Grid>
        <Grid Grid.Row="2">
            <TextBox x:Name="DepartmentShortTitle" 
                     Grid.Row="1"
                     Style="{StaticResource ItemTitleBox}"
                     VerticalAlignment="Top"
                     FontSize="14"
                     Text="{Binding DepartmentShortTitle}"
                     Background="WhiteSmoke"
                     SpellCheck.IsEnabled="True"/>
            <Label IsHitTestVisible="False" 
                   Grid.Row="1"
                   Foreground="DimGray"
                   Content="Введите сокращенное наименование отдела..."
                   VerticalAlignment="Top"
                   FontSize="14"
                   Visibility="{Binding Text.IsEmpty, ElementName=DepartmentShortTitle, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Margin="30 10"/>
        </Grid>
        <Grid Grid.Row="3">
            <Button Content="Удалить отдел" 
                    HorizontalAlignment="Left"
                    Style="{StaticResource Buttons}"
                    Padding="10 5"
                    Margin="20 5"
                    FontFamily="Arial" 
                    FontSize="14"
                    FontWeight="Bold"
                    Command="{Binding IDelete}"/>
            <Button Content="Сохранить изменения" 
                    HorizontalAlignment="Right"
                    Style="{StaticResource Buttons}"
                    Padding="10, 5"
                    Margin="20 5"
                    FontFamily="Arial" 
                    FontSize="14"
                    FontWeight="Bold"
                    Command="{Binding ISave}"/>
        </Grid>
        <Grid Grid.Row="4">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF7D00FF" Offset="0"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
        </Grid>
    </Grid>
</Window>
