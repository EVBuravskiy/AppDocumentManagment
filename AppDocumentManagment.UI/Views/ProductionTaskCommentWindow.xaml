<Window x:Class="AppDocumentManagment.UI.Views.ProductionTaskCommentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppDocumentManagment.UI.Views"
        xmlns:ti="clr-namespace:AppDocumentManagment.UI.Views.UserControllers"
        mc:Ignorable="d"
        Title="АИС Документооборот"  
        Height="900"
        Width="600"
        Icon="/Resources/Images/AppIcon.png" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black" Offset="0.778"/>
                <GradientStop Color="White" Offset="0"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="60px"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60px"/>
            <RowDefinition Height="60px"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="#FF7D00FF" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <Label Grid.Row="0" 
                   x:Name="GreetingLabel"
                   Content="Панель комментариев к задаче" 
                   HorizontalAlignment="Left" 
                   Padding="25,0"
                   Style="{StaticResource TopPanelLabel}"
                   Foreground="WhiteSmoke"/>
            <Button x:Name="ExitBtn"
                    Content="Выйти"
                    HorizontalAlignment="Right"
                    Style="{StaticResource Buttons}"
                    Margin="15, 5"
                    Padding="20, 0"
                    Command="{Binding IExit}"/>
        </Grid>
        <Grid Grid.Row="1"
              Margin="0 10 0 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="200px"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0"
                      Margin="20 10">
                    <ListView ItemsSource="{Binding ProductionTaskComments}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ti:ProductionTaskCommentItem Width="530"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
                <Grid Grid.Row="1"
                      Margin="20 10">
                    <TextBox x:Name="TaskComment" 
                             Style="{StaticResource ItemTitleBox}"
                             VerticalAlignment="Top"
                             VerticalContentAlignment="Top"
                             FontSize="14"
                             Text="{Binding ProductionTaskCommentText}"
                             Background="WhiteSmoke"
                             SpellCheck.IsEnabled="True"
                             ToolTip="Введите комментарий..."
                             Height="160"
                             />
                    <Label IsHitTestVisible="False" 
                           Foreground="DimGray"
                           Content="Введите комментарий..."
                           VerticalAlignment="Center"
                           Height="160"
                           FontSize="14"
                           Visibility="{Binding Text.IsEmpty, ElementName=TaskComment, Converter={StaticResource BooleanToVisibilityConverter}}"
                           Margin="30 10"
                           />
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Row="2"
              Margin="0 0 0 0"
              Background="Black">
            <Button Style="{StaticResource Buttons}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Content="Добавить комментарий"
                    Padding="10 10"
                    Margin="20 0"
                    Command="{Binding IAddProductionTaskComment}"/>
        </Grid>
    </Grid>
</Window>